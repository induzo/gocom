name: "Bench http/middleware/idempotency"

on:
  push:
    paths:
      - "http/middleware/idempotency/**"
      - ".github/workflows/http_middleware_idempotency.bench.yml"
      - ".github/workflows/_bench.yml"
    branches: [main]

concurrency:
  group: bench-http-middleware-idempotency-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  deployments: write

jobs:
  bench:
    uses: ./.github/workflows/_bench.yml
    with:
      module: http/middleware/idempotency
    secrets: inherit
